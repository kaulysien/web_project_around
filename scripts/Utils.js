export const openPopup = (popupElement) => {
    // Implementação da função openPopup
  };
  
  export const closePopup = (popupElement) => {
    // Implementação da função closePopup
  };
  
  export const handleViewImageOnPopup = () => {
    // Implementação da função handleViewImageOnPopup
  };
  
  // Outros manipuladores de eventos e funções auxiliares, se houver
  