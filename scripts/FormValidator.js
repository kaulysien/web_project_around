export default class FormValidator {
    constructor(config, formElement) {
      this._config = config;
      this._formElement = formElement;
    }
  
    _showInputError(inputElement, errorMessage) {
      // Implementação do método _showInputError
    }
  
    _hideInputError(inputElement) {
      // Implementação do método _hideInputError
    }
  
    _checkInputValidity(inputElement) {
      // Implementação do método _checkInputValidity
    }
  
    _toggleButtonState() {
      // Implementação do método _toggleButtonState
    }
  
    _setEventListeners() {
      // Implementação do método _setEventListeners
    }
  
    enableValidation() {
      // Implementação do método enableValidation
    }
  }
  